name: Auto Sync Files

on:
  schedule:
    # Run every minute
    - cron: '* * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  sync-files:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0 # Fetch full history for proper git operations
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 10
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'pnpm'
    
    
    
    - name: Install dependencies and link CLI
      run: |
        pnpm install
        pnpm run link-all
    
    - name: Configure Git
      run: |
        git config --global user.name "VegaShare Bot"
        git config --global user.email "vegashare-bot@noreply.github.com"
    
    - name: Run auto sync
      env:
        SYNC_TIMEOUT_SECONDS: ${{ vars.SYNC_TIMEOUT_SECONDS || '18000' }}
      run: |
        # Use the globally linked vega-cli command
        echo "🔄 Starting auto sync with timeout: ${SYNC_TIMEOUT_SECONDS} seconds"
        vega-cli check-update --extract --output ./vega --url "https://vegaftp.free.nf/?i=1&seconds=${SYNC_TIMEOUT_SECONDS}"
        
        # Debug: List extracted files
        echo "📁 Contents of ./vega directory:"
        ls -la ./vega/ || echo "❌ ./vega directory not found"
        
        # Debug: Check if files exist in expected locations
        echo "🔍 Checking for specific files:"
        find ./vega -name "*.veg" -o -name "*.ini" -o -name "*.pgn" | head -10 || echo "❌ No .veg/.ini/.pgn files found"
        
        # Debug: Show git status before any operations
        echo "📊 Git status before processing:"
        git status --porcelain || echo "❌ Git status failed"
    
    - name: Check for changes
      id: check-changes
      run: |
        echo "🔍 Checking for git changes..."
        
        # Show detailed git status
        echo "📊 Full git status:"
        git status --porcelain || echo "❌ Git status failed"
        
        # Show untracked files
        echo "📁 Untracked files:"
        git ls-files --others --exclude-standard || echo "❌ No untracked files"
        
        # Show modified files
        echo "📝 Modified files:"
        git diff --name-only || echo "❌ No modified files"
        
        # Check if there are any changes
        if [ -n "$(git status --porcelain)" ]; then
          echo "✅ Changes detected"
          echo "changes=true" >> $GITHUB_OUTPUT
        else
          echo "ℹ️ No changes detected"
          echo "changes=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Commit and push changes
      if: steps.check-changes.outputs.changes == 'true'
      run: |
        git add .
        git commit -m "Auto-sync: Update files from remote server
        
        - Synced files changed in the last 5 minutes
        - Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
        - Workflow: ${{ github.workflow }}
        - Run ID: ${{ github.run_id }}"
        git push origin main
    
    - name: Trigger HTML Sync
      if: steps.check-changes.outputs.changes == 'true'
      uses: actions/github-script@v7
      with:
        script: |
          console.log('🚀 Triggering HTML sync workflow after auto-sync...');
          await github.rest.actions.createWorkflowDispatch({
            owner: context.repo.owner,
            repo: context.repo.repo,
            workflow_id: 'sync-html.yml',
            ref: context.ref
          });
          console.log('✅ HTML sync workflow triggered successfully');
    
    - name: Log completion
      run: |
        if [ "${{ steps.check-changes.outputs.changes }}" == "true" ]; then
          echo "✅ Files synced and committed successfully"
        else
          echo "ℹ️ No changes detected, skipping commit"
        fi
